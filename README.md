# YouTube Downloader 🎬🎵

Удобный Python-скрипт для скачивания видео и аудио с YouTube в максимальном качестве. Поддерживает отдельные видео и целые плейлисты.

## ✨ Особенности

- 📺 **Высокое качество**: MP4 до 1080p, MP3 до 320 kbps
- 📋 **Поддержка плейлистов**: Скачивайте целые плейлисты одной командой
- 🎯 **Выборочная загрузка**: Указывайте конкретные видео из плейлиста
- 🍪 **Автоматические cookies**: Использует cookies из браузера для обхода ограничений
- 📊 **Прогресс в реальном времени**: Видите скорость и время скачивания
- 🎵 **Два формата одновременно**: Опция `--format both` сохраняет и MP4, и MP3
- 🗂️ **Умная организация**: Автоматически создает папки для плейлистов с нумерацией

## 🎬 Качество

- **MP4**: До 1080p (Full HD) - автоматически выбирает лучшее доступное
- **MP3**: 320 kbps - максимальное качество звука

## 📦 Установка

### Требования

- Python 3.9+
- FFmpeg (для конвертации в MP3)

### Шаг 1: Клонировать репозиторий

```bash
git clone https://github.com/YOUR_USERNAME/youtube-downloader.git
cd youtube-downloader
```

> ⚠️ Замените `YOUR_USERNAME` на ваше имя пользователя GitHub

### Шаг 2: Установить зависимости

```bash
pip3 install yt-dlp
```

### Шаг 3: Установить FFmpeg

**macOS:**
```bash
brew install ffmpeg
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update && sudo apt install ffmpeg
```

**Windows:**
Скачайте с [официального сайта](https://ffmpeg.org/download.html)

## 🚀 Использование

### Простой запуск:
```bash
./yt-download.sh "URL_видео" --format both
```

### Или напрямую через Python:
```bash
python3 youtube_downloader.py "URL" --format both
```

## 📋 Примеры использования

### Одно видео

**Скачать и MP4, и MP3:**
```bash
python3 youtube_downloader.py "https://www.youtube.com/watch?v=VIDEO_ID" --format both
```

**Только видео (MP4):**
```bash
python3 youtube_downloader.py "URL" --format mp4
```

**Только аудио (MP3):**
```bash
python3 youtube_downloader.py "URL" --format mp3
```

### Плейлисты

**Скачать весь плейлист:**
```bash
python3 youtube_downloader.py "https://www.youtube.com/playlist?list=PLAYLIST_ID" --format both
```

**Первые 5 видео из плейлиста:**
```bash
python3 youtube_downloader.py "PLAYLIST_URL" --items "1-5" --format mp3
```

**Конкретные видео (1, 3, 5, 7):**
```bash
python3 youtube_downloader.py "PLAYLIST_URL" --items "1,3,5,7" --format mp4
```

**Диапазоны видео:**
```bash
python3 youtube_downloader.py "PLAYLIST_URL" --items "10-20,25,30-35" --format both
```

### Дополнительные опции

**Указать папку для сохранения:**
```bash
python3 youtube_downloader.py "URL" --format mp4 --output ~/Videos
```

**Не создавать подпапку для плейлиста:**
```bash
python3 youtube_downloader.py "PLAYLIST_URL" --format mp4 --no-subfolder
```

## 📁 Структура

```
youtube_downloader/
├── youtube_downloader.py        # Основной скрипт
├── yt-download.sh              # Удобный запускалка (опционально)
└── README.md                   # Документация
```

## 💾 Куда сохраняется

По умолчанию: `~/Downloads/`

Для плейлистов автоматически создается подпапка с названием:
```
~/Downloads/
└── Название плейлиста/
    ├── 01 - Первое видео.mp4
    ├── 01 - Первое видео.mp3
    ├── 02 - Второе видео.mp4
    └── 02 - Второе видео.mp3
```

## ⚙️ Параметры командной строки

| Параметр | Короткий | Описание | По умолчанию |
|----------|----------|----------|--------------|
| `--format` | `-f` | Формат: `mp4`, `mp3` или `both` | `mp4` |
| `--output` | `-o` | Папка для сохранения | `~/Downloads` |
| `--items` | `-i` | Номера видео из плейлиста | Все |
| `--no-subfolder` | - | Не создавать папку для плейлиста | `False` |

## 🔍 Примеры форматов `--items`

- `"1-5"` - видео с 1 по 5
- `"1,3,5"` - видео 1, 3 и 5
- `"1-10,15,20-25"` - видео 1-10, 15 и 20-25

## 🛠️ Возможные проблемы

### Ошибка: "Video unavailable"
Некоторые видео могут быть недоступны или приватные. Скрипт автоматически пропускает их и продолжает скачивание.

### Ошибка: "HTTP Error 403"
Попробуйте:
1. Убедитесь, что у вас в браузере открыт YouTube
2. Скрипт автоматически использует cookies из браузера
3. Поддерживаемые браузеры: Chrome, Firefox, Safari, Edge, Opera, Brave

### FFmpeg не найден
Убедитесь, что FFmpeg установлен и доступен в PATH:
```bash
ffmpeg -version
```

## 📝 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 🤝 Вклад

Contributions, issues и feature requests приветствуются!

---

⭐ Если проект оказался полезным, поставьте звездочку!
